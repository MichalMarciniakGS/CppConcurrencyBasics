The first small example focuses on:

- creating threads with std::thread

- comparing std::thread and std::jthread

- observing interleaved output with plain std::cout

- using std::osyncstream for cleaner concurrent console output